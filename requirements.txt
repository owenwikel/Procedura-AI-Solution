# Versions installed in .venv when this was written (Python 3.11).
# scripts/height_coverage_check.py itself only needs requests.
requests==2.34.2
shapely==2.1.2
pyproj==3.7.2

# perception/segment.py (Grounding DINO + SAM 2 via transformers).
# torch was installed from the CUDA 12.4 wheel index (2.6.0+cu124); on a CUDA
# machine use: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
numpy==2.4.6
opencv-python==5.0.0.93
pillow==12.3.0
scipy==1.17.1
torch>=2.6
torchvision>=0.21
transformers==5.17.0

# perception/render_compare.py
trimesh==5.1.0

# confidence/features.py, train.py
scikit-learn==1.9.1
matplotlib==3.11.2
